<h1>Save any kind of movies</h1>

	<div class="container">
		<div class="d-flex align-items-center justify-content-between mb-2">
			<h2> My Lists </h2>
			<% link_to 'Creat a Movie List', new_list_path, class:'btn btn-flat' %>
		</div>
	</div>
	<div class="row">
		<% @list.each do |list| %>
			<% link_to list_path(list), class:'text-decoration-none'do %>
			<% list.name %>
	</div>
	<% end %>
<% end %>	